# POST /api/sensors/
Recebe dados do ESP32.

# POST /api/pair/
Associa estendal ao utilizador autenticado.

# POST /api/control/
Controla o estado do estendal (manual).

# GET /api/device/state
Envia para o ESP32, ou seja, o ESP32 recebe o último valor do estado do estendal